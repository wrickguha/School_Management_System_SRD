# The Base URL of the Laravel REST API.
# If hosting the frontend and backend on the same domain/folder (unified setup),
# you can set this to '/api' or use the full path.
VITE_API_URL=https://subhraedu.com/api
